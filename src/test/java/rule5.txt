
arr = [1, 2, 3];


map = {name: 'aaaaa', age: 15};

rson = {
    name: fun(name, age) {
        println(name, age);
    }
};

rson.name('lsc', 15);

println(arr, arr[0], arr[100]);

println(map, map.name, map.age);