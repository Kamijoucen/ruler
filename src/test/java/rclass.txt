

var createRClass = fun() ->
{
    ToString: fun(self) {
        if typeof(self) == 'string' {
            return self;
        }
        else if typeof(self) == 'array' {
            var str = '';
            for i in self {
                str += i;
            }
        }
        else if typeof(self) == '' {

        }
    },

    ToString2: match self {
    | typeof 'string' -> self
    | typeof 'array'  -> {
        var str = "";
        for i in self {
            str += i;
        }
    }
    }
};

match self {
| typeof 'array' -> self.stream().map(fun(it) -> it.ToString()).collect();
| typeof 'array' ->
}

var r = match (result, succe) {
| (typeof 'array' && "%success%", _) -> "ok"
| (_, 'error') -> "error"
}
