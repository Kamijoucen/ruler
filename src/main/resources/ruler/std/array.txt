
fun IsArray(arr) {
    return (typeof arr) == "array";
}

fun Size(arr) {
    if !IsArray(arr) {
        return null;
    }
    return length(arr);
}

fun IsEmpty(list) {
    if list == null {
        return true;
    }
    if !IsArray(arr) {
        return false;
    }
    return length(arr) == 0;
}

fun IsNotEmpty(list) {
    return !IsEmpty(list);
}
